import { createFileRoute, Link } from "@tanstack/react-router";
import { AlertTriangle, BookOpenText, Mail, MessageSquareText } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { useLocale } from "@/lib/locale";
export const Route=createFileRoute("/support")({head:()=>({meta:[{title:"Support — GlobeTrotr"},{name:"description",content:"Find GlobeTrotr help, product updates, known issues and contact details."}]}),component:Support});
function Support(){const{text}=useLocale();const items=[{icon:MessageSquareText,title:text("Feedback uit de app","In-app feedback"),body:text("Ingelogde betatesters kunnen via de feedbackknop direct een probleem of idee insturen.","Signed-in beta testers can submit an issue or idea using the feedback button."),to:"/beta" as const},{icon:AlertTriangle,title:text("Bekende problemen","Known issues"),body:text("Controleer actuele beperkingen, status en tijdelijke oplossingen.","Review current limitations, status and workarounds."),to:"/known-issues" as const},{icon:BookOpenText,title:text("Productupdates","Product updates"),body:text("Bekijk per betaversie wat is toegevoegd en verbeterd.","See what was added and improved in each beta release."),to:"/updates" as const}];return <div className="space-y-10 pb-8"><section className="aurora rounded-3xl p-8 sm:p-12"><h1 className="font-display text-4xl font-semibold">{text("Hulp en contact","Help and contact")}</h1><p className="mt-4 max-w-2xl text-sm leading-relaxed opacity-85">{text("Vind actuele informatie of neem rechtstreeks contact op. Vermeld bij een technisch probleem wat je deed, welk apparaat je gebruikte en wat je verwachtte.","Find current information or contact us directly. For technical problems, include what you did, the device you used and what you expected.")}</p></section><div className="grid gap-4 md:grid-cols-3">{items.map(({icon:Icon,title,body,to})=><Card key={title} className="surface"><CardContent className="p-6"><Icon className="size-6 text-primary"/><h2 className="mt-4 font-display text-xl font-semibold"><Link to={to}>{title}</Link></h2><p className="mt-2 text-sm text-muted-foreground">{body}</p></CardContent></Card>)}</div><Card className="surface"><CardContent className="flex flex-col gap-4 p-6 sm:flex-row sm:items-center"><span className="grid size-12 place-items-center rounded-2xl bg-primary/10 text-primary"><Mail className="size-5"/></span><div><h2 className="font-display text-xl font-semibold">info@globetrotr.nl</h2><p className="mt-1 text-sm text-muted-foreground">{text("Voor productvragen, klachten en ondersteuning.","For product questions, complaints and support.")}</p></div><a className="sm:ml-auto text-sm font-medium text-primary underline" href="mailto:info@globetrotr.nl">{text("Stuur een e-mail","Send an email")}</a></CardContent></Card></div>}
