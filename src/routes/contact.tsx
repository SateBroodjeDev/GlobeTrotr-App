import{createFileRoute}from"@tanstack/react-router";import{useState}from"react";import{Mail,Send}from"lucide-react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{Card,CardContent}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Textarea}from"@/components/ui/textarea";import{useLocale}from"@/lib/locale";import{submitContact}from"@/lib/contact.functions";
export const Route=createFileRoute("/contact")({component:Page});function Page(){const{text,locale}=useLocale();const[f,setF]=useState({name:"",email:"",subject:"",message:"",website:""});const[busy,setBusy]=useState(false);async function send(){setBusy(true);try{await submitContact({data:{...f,locale,captchaToken:""}});setF({name:"",email:"",subject:"",message:"",website:""});toast.success(text("Bericht ontvangen.","Message received."));}catch{toast.error(text("Bericht kon niet worden verzonden.","Message could not be sent."));}finally{setBusy(false)}}return <div className="mx-auto max-w-4xl space-y-8"><header className="aurora rounded-3xl p-8 sm:p-12"><Mail className="size-8 text-primary"/><h1 className="mt-4 font-display text-4xl font-semibold">{text("Neem contact op","Contact GlobeTrotr")}</h1><p className="mt-3 max-w-2xl opacity-85">{text("Een vraag, klacht of idee? We lezen ieder bericht en reageren via e-mail.","Question, complaint or idea? We read every message and reply by email.")}</p></header><Card><CardContent className="grid gap-4 p-6 sm:grid-cols-2"><Input placeholder={text("Naam","Name")} value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><Input type="email" placeholder="E-mail" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><Input className="sm:col-span-2" placeholder={text("Onderwerp","Subject")} value={f.subject} onChange={e=>setF({...f,subject:e.target.value})}/><Textarea className="sm:col-span-2" rows={7} maxLength={3000} placeholder={text("Waar kunnen we mee helpen?","How can we help?")} value={f.message} onChange={e=>setF({...f,message:e.target.value})}/><input className="hidden" tabIndex={-1} autoComplete="off" value={f.website} onChange={e=>setF({...f,website:e.target.value})}/><div className="sm:col-span-2 flex items-center justify-between gap-4"><p className="text-xs text-muted-foreground">info@globetrotr.nl</p><Button disabled={busy||f.message.length<20} onClick={()=>void send()}><Send className="size-4"/>{text("Versturen","Send")}</Button></div></CardContent></Card></div>}
