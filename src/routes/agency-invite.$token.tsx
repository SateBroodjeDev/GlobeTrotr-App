import { createFileRoute } from "@tanstack/react-router";
import { AgencyInvitationPage } from "./agency-uitnodiging.$token";
export const Route=createFileRoute("/agency-invite/$token")({component:AgencyInvitationPage});
