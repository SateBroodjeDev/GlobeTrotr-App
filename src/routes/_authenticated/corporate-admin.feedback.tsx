import{createFileRoute}from"@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{useState}from"react";import{Archive,RotateCcw,Trash2}from"lucide-react";import{toast}from"sonner";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{issueCategoryLabel,ISSUE_CATEGORIES}from"@/lib/issue-categories";import{getCorporateAdminData,manageAdminRecord,updateFeedbackStatus}from"@/lib/issues.functions";import{statusLabel}from"@/lib/corporate-admin-ui";import{useLocale}from"@/lib/locale";
export const Route=createFileRoute("/_authenticated/corporate-admin/feedback")({component:FeedbackPage});
function FeedbackPage(){const{text}=useLocale();const query=useQuery({queryKey:["corporate-admin-data"],queryFn:()=>getCorporateAdminData()});const[search,setSearch]=useState("");const[category,setCategory]=useState("all");const[archived,setArchived]=useState(false);async function manage(id:string,action:"archive"|"restore"|"delete"){if(action==="delete"&&!confirm(text("Definitief verwijderen?","Permanently delete?")))return;try{await manageAdminRecord({data:{kind:"feedback",id,action}});await query.refetch();}catch{toast.error(text("Actie mislukt.","Action failed."));}}const rows=(query.data?.feedback??[]).filter((item:any)=>(archived||!item.archived_at)&&(category==="all"||item.category===category)&&`${item.title} ${item.description}`.toLowerCase().includes(search.toLowerCase()));return <div className="space-y-4"><Card><CardContent className="grid gap-3 p-4 sm:grid-cols-[1fr_14rem_auto]"><Input value={search} onChange={e=>setSearch(e.target.value)} placeholder={text("Zoeken…","Search…")}/><select className="h-10 rounded-md border bg-background px-3" value={category} onChange={e=>setCategory(e.target.value)}><option value="all">{text("Alle categorieën","All categories")}</option>{ISSUE_CATEGORIES.map(v=><option key={v} value={v}>{issueCategoryLabel(v,text)}</option>)}</select><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={archived} onChange={e=>setArchived(e.target.checked)}/>{text("Archief tonen","Show archive")}</label></CardContent></Card><Card><CardHeader><CardTitle>{text("Ontvangen feedback","Received feedback")}</CardTitle></CardHeader><CardContent className="space-y-3">{rows.map((item:any)=><div key={item.id} className="rounded-xl border p-4"><div className="flex flex-wrap items-center gap-2"><strong>{item.title}</strong><Badge>{issueCategoryLabel(item.category,text)}</Badge><select className="rounded-md border bg-background px-2 py-1 text-xs" value={item.status} onChange={async e=>{await updateFeedbackStatus({data:{id:item.id,status:e.target.value as any}});await query.refetch();}}>{["new","reviewing","planned","resolved","closed"].map(v=><option key={v} value={v}>{statusLabel(v,text)}</option>)}</select>{item.archived_at&&<Badge>{text("Gearchiveerd","Archived")}</Badge>}<span className="ml-auto flex gap-1"><Button size="icon" variant="outline" onClick={()=>void manage(item.id,item.archived_at?"restore":"archive")}>{item.archived_at?<RotateCcw className="size-4"/>:<Archive className="size-4"/>}</Button><Button size="icon" variant="destructive" onClick={()=>void manage(item.id,"delete")}><Trash2 className="size-4"/></Button></span></div><p className="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{item.description}</p><p className="mt-2 text-xs text-muted-foreground">{item.page_url}</p></div>)}</CardContent></Card></div>}
