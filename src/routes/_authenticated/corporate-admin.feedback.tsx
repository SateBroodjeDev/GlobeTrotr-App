import{createFileRoute}from"@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{useState}from"react";import{Archive,RotateCcw,Trash2}from"lucide-react";import{toast}from"sonner";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Textarea}from"@/components/ui/textarea";import{issueCategoryLabel,ISSUE_CATEGORIES}from"@/lib/issue-categories";import{getCorporateAdminData,manageAdminRecord,replyToFeedback,updateFeedbackStatus}from"@/lib/issues.functions";import{statusLabel}from"@/lib/corporate-admin-ui";import{useLocale}from"@/lib/locale";
export const Route=createFileRoute("/_authenticated/corporate-admin/feedback")({component:FeedbackPage});
function FeedbackPage(){const{text}=useLocale();const query=useQuery({queryKey:["corporate-admin-data"],queryFn:()=>getCorporateAdminData()});const[search,setSearch]=useState("");const[category,setCategory]=useState("all");const[archived,setArchived]=useState(false);const[reply,setReply]=useState<Record<string,string>>({});async function answer(id:string){const body=(reply[id]??"").trim();if(!body)return;try{await replyToFeedback({data:{id,body}});setReply({...reply,[id]:""});await query.refetch();toast.success(text("Reactie opgeslagen en gebruiker geïnformeerd.","Reply saved and user notified."));}catch{toast.error(text("Reactie kon niet worden opgeslagen.","Reply could not be saved."));}}async function manage(id:string,action:"archive"|"restore"|"delete"){if(action==="delete"&&!confirm(text("Definitief verwijderen?","Permanently delete?")))return;try{await manageAdminRecord({data:{kind:"feedback",id,action}});await query.refetch();}catch{toast.error(text("Actie mislukt.","Action failed."));}}const rows=(query.data?.feedback??[]).filter((item:any)=>(archived||!item.archived_at)&&(category==="all"||item.category===category)&&`${item.title} ${item.description}`.toLowerCase().includes(search.toLowerCase()));return <div className="space-y-4"><Card><CardContent className="grid gap-3 p-4 sm:grid-cols-[1fr_14rem_auto]"><Input value={search} onChange={e=>setSearch(e.target.value)} placeholder={text("Zoeken…","Search…")}/><select className="h-10 rounded-md border bg-background px-3" value={category} onChange={e=>setCategory(e.target.value)}><option value="all">{text("Alle categorieën","All categories")}</option>{ISSUE_CATEGORIES.map(v=><option key={v} value={v}>{issueCategoryLabel(v,text)}</option>)}</select><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={archived} onChange={e=>setArchived(e.target.checked)}/>{text("Archief tonen","Show archive")}</label></CardContent></Card><Card><CardHeader><CardTitle>{text("Ontvangen feedback","Received feedback")}</CardTitle></CardHeader><CardContent className="space-y-3">{rows.map((item:any)=><div key={item.id} className="rounded-xl border p-4"><div className="flex flex-wrap items-center gap-2"><strong>{item.title}</strong><Badge>{issueCategoryLabel(item.category,text)}</Badge><select className="rounded-md border bg-background px-2 py-1 text-xs" value={item.status} onChange={async e=>{await updateFeedbackStatus({data:{id:item.id,status:e.target.value as any}});await query.refetch();}}>{["new","reviewing","planned","resolved","closed"].map(v=><option key={v} value={v}>{statusLabel(v,text)}</option>)}</select>{item.archived_at&&<Badge>{text("Gearchiveerd","Archived")}</Badge>}<span className="ml-auto flex gap-1"><Button size="icon" variant="outline" aria-label={item.archived_at?text("Herstellen","Restore"):text("Archiveren","Archive")} onClick={()=>void manage(item.id,item.archived_at?"restore":"archive")}>{item.archived_at?<RotateCcw className="size-4"/>:<Archive className="size-4"/>}</Button><Button size="icon" variant="destructive" aria-label={text("Verwijderen","Delete")} onClick={()=>void manage(item.id,"delete")}><Trash2 className="size-4"/></Button></span></div><p className="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{item.description}</p><p className="mt-2 text-xs text-muted-foreground">{item.page_url}</p><div className="mt-4 space-y-2 border-t pt-3">{(query.data?.feedbackReplies??[]).filter((row:any)=>row.feedback_id===item.id).map((row:any)=><div key={row.id} className="rounded-lg bg-muted/50 p-3 text-sm"><p className="whitespace-pre-wrap">{row.body}</p><p className="mt-1 text-xs text-muted-foreground">{new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(new Date(row.created_at))}</p></div>)}<Textarea value={reply[item.id]??""} maxLength={2000} placeholder={text("Schrijf een reactie of aanvullende vraag…","Write a reply or follow-up question…")} onChange={e=>setReply({...reply,[item.id]:e.target.value})}/><Button size="sm" disabled={(reply[item.id]??"").trim().length<2} onClick={()=>void answer(item.id)}>{text("Reactie versturen","Send reply")}</Button></div></div>)}</CardContent></Card></div>}
