import{createFileRoute}from"@tanstack/react-router";import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{CheckCircle2,Circle,ClipboardCheck}from"lucide-react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{getInfrastructureAdminData,updateReleaseChecklistItem}from"@/lib/platform-operations.functions";import{useLocale}from"@/lib/locale";
export const Route=createFileRoute("/_authenticated/corporate-admin/release-checklist")({component:Page});
function Page(){const{text,locale}=useLocale();const qc=useQueryClient();const query=useQuery({queryKey:["infrastructure-admin"],queryFn:()=>getInfrastructureAdminData()});const mutation=useMutation({mutationFn:(item:any)=>updateReleaseChecklistItem({data:{itemKey:item.item_key,completed:!item.completed_at,notes:item.notes??""}}),onSuccess:async()=>{await qc.invalidateQueries({queryKey:["infrastructure-admin"]})},onError:()=>toast.error(text("Checklist kon niet worden bijgewerkt.","Checklist could not be updated."))});const items=query.data?.checklist??[];const done=items.filter((item:any)=>item.completed_at).length;return <Card><CardHeader><CardTitle className="flex items-center gap-2"><ClipboardCheck className="size-5"/>{text("Implementatie- en testchecklist","Implementation and test checklist")}</CardTitle><p className="text-sm text-muted-foreground">{done}/{items.length} {text("afgerond. Technische controles en handmatige praktijktests blijven afzonderlijk zichtbaar.","completed. Technical checks and manual acceptance tests remain separately visible.")}</p></CardHeader><CardContent className="space-y-3">{items.map((item:any)=><div key={item.item_key} className={`flex items-start gap-3 rounded-xl border p-4 ${item.completed_at?"bg-emerald-500/5":""}`}><Button size="icon" variant="ghost" disabled={mutation.isPending} onClick={()=>mutation.mutate(item)} aria-label={text("Status wijzigen","Change status")}>{item.completed_at?<CheckCircle2 className="size-5 text-emerald-600"/>:<Circle className="size-5"/>}</Button><div className="min-w-0"><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">{item.category}</p><p className="mt-1 font-medium">{locale==="en"?item.label_en:item.label_nl}</p>{item.notes&&<p className="mt-1 text-xs text-muted-foreground">{item.notes}</p>}{item.completed_at&&<p className="mt-1 text-xs text-muted-foreground">{text("Vastgelegd op","Recorded on")} {new Intl.DateTimeFormat(locale==="en"?"en-GB":"nl-NL",{dateStyle:"medium",timeStyle:"short"}).format(new Date(item.completed_at))}</p>}</div></div>)}</CardContent></Card>}
