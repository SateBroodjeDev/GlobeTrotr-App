import{createFileRoute}from"@tanstack/react-router";import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{CheckCircle2,ChevronDown,Circle,ClipboardCheck}from"lucide-react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Progress}from"@/components/ui/progress";import{getInfrastructureAdminData,updateReleaseChecklistItem}from"@/lib/platform-operations.functions";import{useLocale}from"@/lib/locale";
const categoryEn:Record<string,string>={"Account":"Account","Reizen":"Trips","Uitgaven":"Expenses","Samenwerking":"Collaboration","Openbaar delen":"Public sharing","Agency":"Agency","Corporate Admin":"Corporate Admin","Publiek":"Public website","Communicatie":"Communication","Release":"Release","Gebruikservaring":"User experience","Beveiliging":"Security"};
export const Route=createFileRoute("/_authenticated/corporate-admin/release-checklist")({component:Page});
function Page(){const{text,locale}=useLocale();const qc=useQueryClient();const query=useQuery({queryKey:["infrastructure-admin"],queryFn:()=>getInfrastructureAdminData()});const mutation=useMutation({mutationFn:(item:any)=>updateReleaseChecklistItem({data:{itemKey:item.item_key,completed:!item.completed_at,notes:item.notes??""}}),onSuccess:async()=>{await qc.invalidateQueries({queryKey:["infrastructure-admin"]})},onError:()=>toast.error(text("Checklist kon niet worden bijgewerkt.","Checklist could not be updated."))});const items=query.data?.checklist??[];const done=items.filter((item:any)=>item.completed_at).length;const groups=Array.from(new Set(items.map((item:any)=>item.category))) as string[];return <div className="space-y-5"><Card><CardHeader><CardTitle className="flex items-center gap-2"><ClipboardCheck className="size-5"/>{text("Implementatie- en testchecklist","Implementation and test checklist")}</CardTitle><p className="text-sm text-muted-foreground">{text("Vink alleen af wat je werkelijk praktisch hebt gecontroleerd. Voeg afwijkingen toe aan Bekende problemen of Feedback.","Only check items you have actually verified in practice. Record deviations under Known issues or Feedback.")}</p></CardHeader><CardContent><div className="flex items-center justify-between text-sm"><span>{done}/{items.length} {text("afgerond","completed")}</span><strong>{items.length?Math.round(done/items.length*100):0}%</strong></div><Progress value={items.length?done/items.length*100:0} className="mt-3"/></CardContent></Card>{query.isLoading&&<Card><CardContent className="p-6 text-sm text-muted-foreground">{text("Checklist laden…","Loading checklist…")}</CardContent></Card>}{groups.map((category,index)=>{const rows=items.filter((item:any)=>item.category===category),categoryDone=rows.filter((item:any)=>item.completed_at).length;return <details key={category} open={index<2} className="group rounded-2xl border bg-card"><summary className="flex cursor-pointer list-none items-center gap-3 p-5"><ChevronDown className="size-5 transition-transform group-open:rotate-180"/><div className="min-w-0 flex-1"><h2 className="font-display text-xl font-semibold">{text(category,categoryEn[category]??category)}</h2><p className="mt-1 text-xs text-muted-foreground">{categoryDone}/{rows.length} {text("afgerond","completed")}</p></div>{categoryDone===rows.length&&rows.length>0&&<CheckCircle2 className="size-5 text-emerald-600"/>}</summary><div className="space-y-2 border-t p-4">{rows.map((item:any)=><div key={item.item_key} className={`flex items-start gap-3 rounded-xl border p-4 ${item.completed_at?"border-emerald-500/25 bg-emerald-500/5":""}`}><Button size="icon" variant="ghost" disabled={mutation.isPending} onClick={()=>mutation.mutate(item)} aria-label={text("Status wijzigen","Change status")}>{item.completed_at?<CheckCircle2 className="size-5 text-emerald-600"/>:<Circle className="size-5"/>}</Button><div className="min-w-0"><p className="font-medium">{locale==="en-GB"?item.label_en:item.label_nl}</p>{item.notes&&<p className="mt-1 text-xs text-muted-foreground">{item.notes}</p>}{item.completed_at&&<p className="mt-1 text-xs text-muted-foreground">{text("Vastgelegd op","Recorded on")} {new Intl.DateTimeFormat(locale==="en-GB"?"en-GB":"nl-NL",{dateStyle:"medium",timeStyle:"short"}).format(new Date(item.completed_at))}</p>}</div></div>)}</div></details>})}</div>}
