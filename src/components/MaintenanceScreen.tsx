import {Link} from "@tanstack/react-router";
import {Activity,Clock3,LogIn,ShieldCheck,Wrench} from "lucide-react";
import {useEffect,useState} from "react";
import {Button} from "@/components/ui/button";
import {useLocale} from "@/lib/locale";

export function MaintenanceScreen({state}:{state:{reasonNl:string;reasonEn:string;endsAt:string|null}}){const{text,locale}=useLocale();const[now,setNow]=useState(Date.now());useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);const remaining=state.endsAt?Math.max(0,Date.parse(state.endsAt)-now):null;const countdown=remaining===null?text("Eindtijd volgt","End time to be confirmed"):format(remaining);return <div className="relative grid min-h-screen place-items-center overflow-hidden bg-background px-5 py-12"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_15%,hsl(var(--primary)/.16),transparent_32%),radial-gradient(circle_at_85%_80%,hsl(var(--accent)/.28),transparent_38%)]"/><main className="relative w-full max-w-4xl overflow-hidden rounded-[2rem] border bg-card/95 shadow-2xl backdrop-blur"><div className="grid lg:grid-cols-[1.15fr_.85fr]"><section className="p-7 sm:p-10 lg:p-12"><span className="grid size-12 place-items-center rounded-2xl bg-primary/10 text-primary"><Wrench className="size-6"/></span><p className="mt-6 text-xs font-semibold uppercase tracking-[.2em] text-primary">GlobeTrotr · {text("gepland onderhoud","scheduled maintenance")}</p><h1 className="mt-3 max-w-xl font-display text-3xl font-semibold leading-tight sm:text-4xl">{text("Even in de werkplaats","Temporarily in the workshop")}</h1><p className="mt-5 max-w-xl text-lg leading-8">{locale==="en-GB"?state.reasonEn:state.reasonNl}</p><p className="mt-4 max-w-xl text-sm leading-6 text-muted-foreground">{text("Je reisgegevens blijven veilig bewaard. Zodra het onderhoud klaar is, kun je verder waar je gebleven was.","Your trip data remains safely stored. Once maintenance is complete, you can continue where you left off.")}</p><div className="mt-8 flex flex-wrap gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><ShieldCheck className="size-4 text-emerald-600"/>{text("Gegevens blijven veilig","Data remains safe")}</span><span className="flex items-center gap-2"><Activity className="size-4 text-primary"/>{text("Status wordt automatisch bijgewerkt","Status updates automatically")}</span></div></section><aside className="border-t bg-muted/30 p-7 sm:p-10 lg:border-l lg:border-t-0"><p className="flex items-center gap-2 text-sm font-medium"><Clock3 className="size-4 text-primary"/>{text("Verwachte resterende tijd","Estimated time remaining")}</p><p className="mt-3 font-display text-3xl font-semibold tabular-nums">{countdown}</p><div className="my-7 h-px bg-border"/><p className="text-sm leading-6 text-muted-foreground">{text("Werk je aan GlobeTrotr? Bevoegde beheerders kunnen tijdens het onderhoud blijven inloggen.","Working on GlobeTrotr? Authorised administrators can continue to sign in during maintenance.")}</p><Button asChild variant="outline" className="mt-5 w-full"><Link to="/auth"><LogIn className="size-4"/>{text("Beheerder inloggen","Administrator sign in")}</Link></Button><p className="mt-6 text-xs text-muted-foreground">{text("Vragen? Mail info@globetrotr.nl","Questions? Email info@globetrotr.nl")}</p></aside></div></main></div>}
function format(ms:number){const total=Math.ceil(ms/1000),days=Math.floor(total/86400),hours=Math.floor(total%86400/3600),minutes=Math.floor(total%3600/60),seconds=total%60;return days?`${days}d ${hours}u ${minutes}m`:hours?`${hours}u ${minutes}m ${seconds}s`:`${minutes}m ${seconds}s`;}
