import {Link} from "@tanstack/react-router";
import {Clock3,LogIn,Wrench} from "lucide-react";
import {useEffect,useState} from "react";
import {Button} from "@/components/ui/button";
import {useLocale} from "@/lib/locale";

export function MaintenanceScreen({state}:{state:{reasonNl:string;reasonEn:string;endsAt:string|null}}){const{text,locale}=useLocale();const[now,setNow]=useState(Date.now());useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);const remaining=state.endsAt?Math.max(0,Date.parse(state.endsAt)-now):null;const countdown=remaining===null?text("Eindtijd volgt","End time to be confirmed"):format(remaining);return <div className="relative grid min-h-screen place-items-center overflow-hidden bg-background px-5 py-12"><div className="absolute inset-0 bg-[radial-gradient(circle_at_top_left,hsl(var(--primary)/.18),transparent_40%),radial-gradient(circle_at_bottom_right,hsl(var(--accent)/.3),transparent_42%)]"/><main className="relative w-full max-w-2xl rounded-[2rem] border bg-card/95 p-7 text-center shadow-2xl backdrop-blur sm:p-12"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-primary/10 text-primary"><Wrench className="size-8"/></span><p className="mt-6 text-sm font-semibold uppercase tracking-[.2em] text-primary">GlobeTrotr maintenance</p><h1 className="mt-3 font-display text-4xl font-semibold sm:text-5xl">{text("We maken GlobeTrotr beter","We are improving GlobeTrotr")}</h1><p className="mx-auto mt-5 max-w-xl leading-7 text-muted-foreground">{locale==="en-GB"?state.reasonEn:state.reasonNl}</p><div className="mx-auto mt-8 max-w-sm rounded-2xl border bg-muted/30 p-5"><p className="flex items-center justify-center gap-2 text-sm text-muted-foreground"><Clock3 className="size-4"/>{text("Verwachte resterende tijd","Estimated time remaining")}</p><p className="mt-2 font-display text-3xl font-semibold tabular-nums">{countdown}</p></div><p className="mt-7 text-sm text-muted-foreground">{text("Bevoegde beheerders kunnen tijdens onderhoud blijven inloggen.","Authorised administrators can continue to sign in during maintenance.")}</p><Button asChild className="mt-4"><Link to="/auth"><LogIn className="size-4"/>{text("Beheerder inloggen","Administrator sign in")}</Link></Button></main></div>}
function format(ms:number){const total=Math.ceil(ms/1000),days=Math.floor(total/86400),hours=Math.floor(total%86400/3600),minutes=Math.floor(total%3600/60),seconds=total%60;return days?`${days}d ${hours}u ${minutes}m`:hours?`${hours}u ${minutes}m ${seconds}s`:`${minutes}m ${seconds}s`;}
