import{useEffect,useState}from"react";import{useQuery}from"@tanstack/react-query";import{Globe,Mail,Save}from"lucide-react";import{toast}from"sonner";import{getAgencyDeliverySettings,saveAgencyDeliverySettings,type AgencyDeliverySettings as Values}from"@/lib/agency-delivery.functions";import{useLocale}from"@/lib/locale";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input}from"@/components/ui/input";
export function AgencyDeliverySettings(){const{text}=useLocale(),q=useQuery({queryKey:["agency-delivery"],queryFn:()=>getAgencyDeliverySettings(),retry:false}),[form,setForm]=useState<Values|null>(null),[busy,setBusy]=useState(false);useEffect(()=>{if(q.data)setForm(q.data)},[q.data]);if(!form)return null;const set=(key:keyof Values,value:string)=>setForm(v=>v?{...v,[key]:value}:v);async function save(){setBusy(true);try{await saveAgencyDeliverySettings({data:{subdomain:form.subdomain,customDomain:form.customDomain,fromName:form.fromName,fromEmail:form.fromEmail,replyTo:form.replyTo}});await q.refetch();toast.success(text("Domein- en afzenderinstellingen opgeslagen.","Domain and sender settings saved."))}catch(e){toast.error(e instanceof Error&&e.message==="DOMAIN_ALREADY_USED"?text("Dit domein is al gekoppeld.","This domain is already linked."):text("Instellingen konden niet worden opgeslagen.","Settings could not be saved."))}finally{setBusy(false)}}return <div className="grid gap-5 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex gap-2"><Globe className="size-5"/>{text("Agency-domein","Agency domain")}</CardTitle></CardHeader><CardContent className="space-y-4"><Field label={text("Subdomein","Subdomain")} value={form.subdomain} suffix=".globetrotr.nl" onChange={v=>set("subdomain",v.toLowerCase().replace(/[^a-z0-9-]/g,""))}/><Field label={text("Eigen domein (optioneel)","Custom domain (optional)")} value={form.customDomain} onChange={v=>set("customDomain",v.toLowerCase())}/><p className="text-xs text-muted-foreground">{text("Na self-hosting verifieert GlobeTrotr een TXT-record en activeert daarna automatisch TLS. Status:","After self-hosting, GlobeTrotr verifies a TXT record and then automatically enables TLS. Status:")} {form.domainStatus}</p>{form.verificationToken&&<code className="block break-all rounded-lg bg-muted p-3 text-xs">globetrotr-verification={form.verificationToken}</code>}</CardContent></Card><Card><CardHeader><CardTitle className="flex gap-2"><Mail className="size-5"/>{text("E-mailafzender","Email sender")}</CardTitle></CardHeader><CardContent className="space-y-4"><Field label={text("Afzendernaam","Sender name")} value={form.fromName} onChange={v=>set("fromName",v)}/><Field label={text("Afzenderadres","From address")} type="email" value={form.fromEmail} onChange={v=>set("fromEmail",v)}/><Field label="Reply-to" type="email" value={form.replyTo} onChange={v=>set("replyTo",v)}/><p className="text-xs text-muted-foreground">{text("SMTP-inloggegevens worden later uitsluitend in de versleutelde VPS secret store gekoppeld en nooit hier getoond.","SMTP credentials will later only be linked in the encrypted VPS secret store and never shown here.")}</p></CardContent></Card><Button className="lg:col-span-2 lg:justify-self-start" disabled={busy} onClick={()=>void save()}><Save className="size-4"/>{busy?text("Opslaan…","Saving…"):text("Domeininstellingen opslaan","Save domain settings")}</Button></div>}
function Field({label,value,onChange,type="text",suffix}:{label:string;value:string;onChange:(v:string)=>void;type?:string;suffix?:string}){return <label className="space-y-1 text-sm"><span>{label}</span><div className="flex"><Input className={suffix?"rounded-r-none":""} type={type} value={value} maxLength={254} onChange={e=>onChange(e.target.value)}/>{suffix&&<span className="flex items-center rounded-r-md border border-l-0 bg-muted px-3 text-xs">{suffix}</span>}</div></label>}
