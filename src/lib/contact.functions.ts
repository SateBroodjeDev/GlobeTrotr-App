import{createServerFn}from"@tanstack/react-start";
export const submitContact=createServerFn({method:"POST"}).validator((x:{name:string;email:string;subject:string;message:string;locale:string;website:string;captchaToken:string})=>x).handler(async({data})=>{if(data.website)throw new Error("SPAM");if(data.name.trim().length<2||data.subject.trim().length<3||data.message.trim().length<20||!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(data.email))throw new Error("INVALID");const secret=process.env["TURNSTILE_SECRET_KEY"]?.trim();if(secret){const body=new URLSearchParams({secret,response:data.captchaToken});const check=await fetch("https://challenges.cloudflare.com/turnstile/v0/siteverify",{method:"POST",body});const result=await check.json() as {success:boolean};if(!result.success)throw new Error("CAPTCHA");}else if(process.env["NODE_ENV"]==="production")throw new Error("CAPTCHA_UNAVAILABLE");const{supabaseAdmin}=await import("@/integrations/supabase/client.server");const{error}=await(supabaseAdmin as any).from("contact_messages").insert({name:data.name.trim(),email:data.email.trim().toLowerCase(),subject:data.subject.trim(),message:data.message.trim(),locale:data.locale});if(error)throw new Error("SAVE_FAILED");return{ok:true};});
