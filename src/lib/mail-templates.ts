export type MailJob={locale:"nl"|"en";templateKey:string;payload:{title?:string;body?:string;tripId?:string|null}};
const escapeHtml=(value:string)=>value.replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[char]!);
export function renderMail(job:MailJob,baseUrl:string){const en=job.locale==="en",title=escapeHtml(job.payload.title?.split(" / ")[en?1:0]??(en?"GlobeTrotr update":"GlobeTrotr-update")),body=escapeHtml(job.payload.body??"").replaceAll("|"," · "),origin=new URL(baseUrl).origin,trip=job.payload.tripId?`${origin}/trips/${encodeURIComponent(job.payload.tripId)}`:origin;return{subject:title,text:`${title}\n\n${job.payload.body??""}\n\n${trip}`,html:`<!doctype html><html lang="${job.locale}"><body style="font-family:Arial,sans-serif;color:#17202a"><main style="max-width:600px;margin:auto;padding:32px"><img src="${origin}/assets/email/logo.png" width="56" height="56" alt="GlobeTrotr" style="display:block;margin-bottom:24px"><h1>${title}</h1><p>${body}</p><p><a href="${trip}">${en?"Open GlobeTrotr":"Open GlobeTrotr"}</a></p><hr><small>${en?"This is a service message from GlobeTrotr.":"Dit is een servicemelding van GlobeTrotr."}</small></main></body></html>`}};
